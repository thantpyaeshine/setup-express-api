# setup-express-api

Scaffold an Express API into the current empty directory.

## Usage

```sh
npx setup-express-api@latest ./my-api
```

For a reproducible scaffold, request a specific version:

```sh
npx setup-express-api@1.0.0 ./my-api
```
